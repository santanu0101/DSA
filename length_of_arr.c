#include <stdio.h>

int main()
{

    int Arr[] = {1, '2', 3, 4, 5};

    // variable to store size of Arr
   for (int i = 0; i < 5; i++)
   {
    printf("The length of the array is: %d\n", Arr[i]);
    
   }
 
    return 0;
}